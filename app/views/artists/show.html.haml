%h1.page-header
  Artists Matching #{params[:artist]}

- # @artists.similar.inspect

%h2 Artist #{params[:artist]}

%p
  = @artists.content


%ul
  - @artists.similar.each do |artist|
    %li
      = artist.name
      | #{link_to "Show", artist_path(:id => artist.name)}
