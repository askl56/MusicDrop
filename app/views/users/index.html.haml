%h1.page-header= @spotify_user.display_name
%ul
  - @spotify_user.playlists.each do |playlist|
    %li
      = playlist.name
      | #{link_to "Show", playlist_path(user: playlist.owner.id, id: playlist.id)}
